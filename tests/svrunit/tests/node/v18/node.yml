commands:
  - name: "node -v should be 18"
    command: "node -v"
    expected: "v18."
  - name: "sudo node -v should be 18"
    command: "sudo node -v"
    not_expected: "command not found" # sudo version switch does not always work, but it needs to be at least found!!!
commands:
  - name: "NODE Version 24"
    command: "node -v"
    expected: "v24."
  - name: "NODE Version 24 as SUDO user"
    command: "sudo node -v"
    not_expected: "command not found" # sudo version switch does not always work, but it needs to be at least found!!!